// Generated by CoffeeScript 1.5.0
(function() {
  var FeaturedImage, media, old_initialize;

  media = this.wp.media;

  FeaturedImage = media.controller.FeaturedImage;

  old_initialize = FeaturedImage.prototype.initialize;

  this.wp.media.controller.FeaturedImage = FeaturedImage.extend({
    initialize: function() {
      if (!this.get('library')) {
        this.set('library', media.query({
          type: ['image', 'video']
        }));
      }
      return old_initialize.call(this);
    }
  });

}).call(this);
